version: '3.8'

services:
  binance-strategy:
    build: .
    environment:
      - BINANCE_API_KEY=${BINANCE_API_KEY}
    volumes:
      - ./data:/app/data
    command: python scripts/fetch_binance_data.py
